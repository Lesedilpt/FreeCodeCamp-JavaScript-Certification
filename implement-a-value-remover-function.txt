** start of script.js **

function destroyer(arr, ...valsToRemove) {
  // Keep only the elements not included in valsToRemove
  return arr.filter(item => !valsToRemove.includes(item));
}


** end of script.js **

