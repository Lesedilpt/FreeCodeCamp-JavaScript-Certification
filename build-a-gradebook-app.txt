** start of script.js **

// 1️⃣ Get average of an array of scores
function getAverage(scores) {
  const total = scores.reduce((sum, score) => sum + score, 0);
  return total / scores.length;
}

// 2️⃣ Get letter grade based on score
function getGrade(score) {
  if (score === 100) return "A+";
  if (score >= 90) return "A";
  if (score >= 80) return "B";
  if (score >= 70) return "C";
  if (score >= 60) return "D";
  return "F";
}

// 3️⃣ Check if grade is passing (anything but "F")
function hasPassingGrade(score) {
  const grade = getGrade(score);
  return grade !== "F";
}

// 4️⃣ Build student message
function studentMsg(scores, studentScore) {
  const average = getAverage(scores);
  const grade = getGrade(studentScore);
  const passed = hasPassingGrade(studentScore);

  return `Class average: ${average}. Your grade: ${grade}. You ${passed ? "passed" : "failed"} the course.`;
}


** end of script.js **

